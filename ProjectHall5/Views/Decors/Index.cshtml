@model IEnumerable<ProjectHall5.Models.Decor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DecorPackage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DecorPrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DecorPackage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DecorPrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DecorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DecorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DecorID })
        </td>
    </tr>
}

</table>
